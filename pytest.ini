[pytest]
markers =
    live: hits real AWS Comprehend Medical; costs money and needs credentials.

# Live tests are opt-in: `py -3.10 -m pytest` stays free and offline.
# Run them with `py -3.10 -m pytest -m live`.
addopts = -m "not live"
